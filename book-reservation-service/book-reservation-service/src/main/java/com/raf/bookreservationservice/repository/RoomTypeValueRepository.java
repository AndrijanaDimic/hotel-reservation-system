package com.raf.bookreservationservice.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.raf.bookreservationservice.domain.RoomTypeValue;

public interface RoomTypeValueRepository extends JpaRepository<RoomTypeValue, Long> {

}
